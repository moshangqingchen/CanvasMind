# 辰途 Flare / Sunburst 2K 复测

北京时间 2026-09-14 09:28–09:30，使用已保存的辰途「低价Adobe生图」分组 Key 读取 `/v1/models`，并重新调用 `/v1/images/generations`。用户接受近似 2K，因此不再以必须精确返回 2048 方图作为唯一开放标准。所有请求使用当前默认最高质量 `max`、`n=1`；下载图片后读取文件实际像素，不以响应参数回显作判断。

共提交 4 次生成请求：Flare 3 次成功，Sunburst 1 次返回 HTTP 503，没有进行无效渠道的重复调用。未测量余额差额，不推断失败请求是否计费。

| 模型 | 比例 | 请求尺寸 | 实际尺寸 | 结果 |
| --- | --- | --- | --- | --- |
| gpt-image-2.5-flare | 1:1 | 2048×2048 | 2048×2048 | 精确匹配 |
| gpt-image-2.5-flare | 16:9 | 2720×1536 | 2720×1536 | 精确匹配 |
| gpt-image-2.5-flare | 9:16 | 1536×2720 | 1536×2720 | 精确匹配 |
| gpt-image-2.5-sunburst | 1:1 | 2048×2048 | 无图片 | 503：当前分组无可用渠道 |

本项目的 2K 档按约 2048² 总像素预算计算，横竖图随比例分配宽高并按 16 像素对齐，并非所有图片都固定最长边 2048。近似结果的本次核验标准为宽、高各在请求值的 ±10% 内，实际比例相对请求比例偏差小于 2%；Flare 三次都精确匹配，没有使用容差放宽通过条件。

Flare 的 2K 现已开放，并提供与现有档位一致的 11 种比例预设及自动比例；本次真实生成覆盖方图、横图、竖图三个代表，其他比例为计算预设，未逐一付费实测。选项注明“实际可能近似”，保留 [9 月 10 日方图返回 1920×1920 的历史记录](chentu-image25-recheck-2026-09-10.md)。当前实测成功不保证每次渠道输出完全相同，也不将本次 max 的表现自动解释为所有质量与尺寸组合均已复测。

Sunburst 在本次 `/v1/models` 列表中已缺失，实际生成返回 `No available channel for model gpt-image-2.5-sunburst under group 低价Adobe生图`。这是当前分组的渠道不可用，不等于该模型永久不支持 2K。本次不新增它的 2K 控件；既有 1K/4K 为历史验证能力，不能保证当前可调用。其他供应商或分组不继承本次结果。

本次只复测文生图，不重新背书图片编辑或 1K/4K。脱敏逐次结果见 [JSON 记录](chentu-2k-recheck-2026-09-14.json)，本地样图保存在 `.codex-temp/chentu-2k-20260914/`，未写入 API Key。
